# Decanter 个人主页

一个现代化的全栈个人主页平台，提供内容展示、互动与管理能力，适合个人品牌与作品集搭建。

## 概览

- 前后端分离架构，前端基于 Vue 3 + TypeScript，后端基于 Koa.js + MongoDB。
- 适配桌面与移动端，内置丰富动效与可视化组件。
- 提供管理后台，支持内容维护与数据统计。

## 功能一览

### 前端
- 主页展示与社交链接
- 友情链接与外部导航
- 留言板与互动反馈
- 关于、赞助、网站状态等内容页
- 管理后台入口

### 后端
- 管理员认证与权限校验
- 友情链接、留言、赞助等数据 CRUD
- 访问日志与统计分析
- 文件上传与资源管理
- 速率限制与安全防护

## 技术栈

### 前端
- Vue 3 / TypeScript / Vite
- Tailwind CSS + SCSS
- Pinia / Vue Router
- Element Plus / GSAP / Canvas Confetti

### 后端
- Node.js / Koa.js
- MongoDB / Mongoose
- Nodemon

### 工具链
- pnpm（Monorepo）
- Docker / Docker Compose
- ESLint + Prettier
- Commitlint + Commitizen

## 目录结构

```
homePage/
├── client/                 # 前端应用
│   ├── src/
│   │   ├── pages/         # 页面组件
│   │   ├── components/    # 可复用组件
│   │   ├── composables/   # 可复用逻辑
│   │   ├── stores/        # Pinia 状态管理
│   │   ├── router/        # 路由配置
│   │   ├── api/           # API 请求
│   │   ├── types/         # TypeScript 类型
│   │   └── utils/         # 工具函数
│   ├── packages/          # 工作区包
│   │   └── webgl-image/   # WebGL 图像处理库
│   └── vite.config.ts     # Vite 配置
│
├── server/                # 后端应用
│   ├── src/
│   │   ├── controllers/   # 控制器
│   │   ├── models/        # 数据模型
│   │   ├── services/      # 服务层
│   │   ├── routes/        # 路由
│   │   ├── middleware/    # 中间件
│   │   ├── config/        # 配置
│   │   └── scripts/       # 脚本
│   └── app.js             # 应用入口
│
└── docker-compose.yml     # Docker 编排
```

## 快速开始

### 环境要求
- Node.js >= 18
- pnpm >= 8
- MongoDB（可选，推荐用 Docker）

### 安装依赖

```bash
pnpm i
```

### 开发模式

```bash
# 同时启动前后端
pnpm dev

# 仅启动前端
pnpm dev:client

# 仅启动后端
pnpm dev:server
```

### 构建与运行

```bash
# 构建前端
pnpm build

# 前端预览
pnpm preview

# 启动后端（开发环境）
pnpm start

# 启动后端（生产环境）
pnpm start:prod
```

## 环境变量

后端会优先读取项目根目录 .env，其次读取 server/.env。前端使用 client/.env 或 client/.env.local。

```env
# =====================
# Client (Vite)
# =====================
VITE_SITE_TITLE="Decanter"
VITE_SITE_DESCRIPTION="Personal site built with Vue + Vite"
VITE_API_BASE_URL="https://serve.giovan.cn"
VITE_API_BASE_URL_LOCAL="http://localhost:8998"
VITE_ENABLE_MOCK=false
VITE_ENABLE_ANALYTICS=false

# =====================
# Server
# =====================
PORT=8998
NODE_ENV=development
SKIP_DB=false

# =====================
# MongoDB
# =====================
MONGODB_URI=mongodb://admin:password@host:27017/database?authSource=admin

# =====================
# Upload
# =====================
UPLOAD_DIR="/Volumes/大金刚/uploads"
UPLOAD_BASE_URL="/uploads"
```

## 上传目录配置

| 配置项 | 说明 | 默认值 |
|-------|------|--------|
| UPLOAD_DIR | 文件保存目录（支持外接硬盘/网络路径） | ./uploads（项目根目录） |
| UPLOAD_BASE_URL | 文件访问前缀 | /uploads |

### Docker 挂载示例

```yaml
services:
  server:
    volumes:
      - /Volumes/大金刚/uploads:/app/uploads
```

## 主要页面

| 页面 | 路径 | 功能描述 |
|------|------|--------|
| 主页 | / | 个人信息展示与社交链接 |
| 关于 | /about | 个人简介与背景 |
| 友情链接 | /friend-links | 友情链接管理与展示 |
| 留言板 | /guestbook | 用户留言与互动 |
| 赞助 | /sponsor | 赞助方式展示 |
| 网站状态 | /status | 服务健康监控 |
| 管理后台 | /admin | 内容管理系统 |

## 脚本速查

| 位置 | 命令 | 说明 |
|------|------|------|
| 根目录 | pnpm dev | 同时启动前后端 |
| 根目录 | pnpm dev:client | 启动前端 |
| 根目录 | pnpm dev:server | 启动后端 |
| 根目录 | pnpm build | 构建前端 |
| 根目录 | pnpm preview | 前端预览 |
| 根目录 | pnpm start | 启动后端（开发） |
| 根目录 | pnpm start:prod | 启动后端（生产） |

## Docker

使用 Docker Compose 运行：

```bash
docker-compose up -d
```

如需完整部署说明，参考 README.docker.md。

## 代码规范

```bash
pnpm commit
```

## 贡献

欢迎提交 Issue 或 Pull Request。

## 致谢

本项目画廊受启发于 [ChronoFrame](https://chronoframe.bh8.ga/)，同样优秀的个人相册项目。

## 许可证

MIT

## 联系方式

- 📧 邮箱：14945447@qq.com
- 💬 微信：14945447
- 📍 地点：中国·四川·成都
- 🌐 网站：https://www.giovan.cn/#/

---

Decanter（醒酒器）希望帮助你打造更有质感的在线形象。
