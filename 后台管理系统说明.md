# homePage 后台管理系统说明

## 1. 系统概述

`homePage` 项目内置了一套后台管理系统，用于支撑个人主页站点的内容维护、权限控制、数据统计与运维辅助能力。

从代码结构看，这套后台并不是一个独立项目，而是：

- **前端后台界面**：集成在 `client` 中，路由前缀为 `/admin`
- **后端管理接口**：集成在 `server` 中，主要以 `/api/admin` 为前缀
- **认证与扫码登录能力**：由 `/api/auth` 提供
- **文件管理能力**：由 `/api/files` 提供

整体属于一个“前后端分离、同仓库管理”的后台管理系统。

---

## 2. 技术架构

### 前端

- Vue 3
- TypeScript
- Vue Router
- Pinia
- Element Plus
- Tailwind CSS

### 后端

- Node.js
- Koa.js
- MongoDB / Mongoose

### 典型职责划分

- `client/src/pages/admin/*`：后台页面
- `client/src/stores/auth.ts`：后台登录态与菜单状态管理
- `client/src/router/index.ts`：后台路由定义与访问守卫
- `server/src/routes/modules/*`：后台接口路由
- `server/src/controllers/*`：后台业务控制器
- `server/src/middleware/adminAuth.js`：后台鉴权中间件

---

## 3. 后台前端入口与页面结构

### 登录入口

后台登录页：

- 路径：`/admin/login`
- 页面文件：`client/src/pages/admin/AdminLogin.vue`

该页面支持两种登录方式：

1. **账号密码登录**
2. **微信小程序二维码登录**

### 后台主入口

后台主框架页：

- 路径：`/admin`
- 页面文件：`client/src/pages/admin/AdminLayout.vue`

该页面负责：

- 顶部导航栏
- 侧边菜单
- 当前用户信息展示
- 主题切换
- 路由子页面承载
- 上传任务浮窗展示

### 已定义的后台页面

根据 `client/src/router/index.ts`，当前后台包含以下页面：

| 菜单/页面 | 路由 | 页面文件 |
|---|---|---|
| 仪表板 | `/admin` | `DashboardPage.vue` |
| 用户管理 | `/admin/users` | `UsersPage.vue` |
| 角色管理 | `/admin/roles` | `RolesPage.vue` |
| 菜单管理 | `/admin/menus` | `MenusPage.vue` |
| 留言管理 | `/admin/messages` | `MessagesPage.vue` |
| 文章管理 | `/admin/articles` | `ArticlesPage.vue` |
| 相册管理 | `/admin/photos` | `PhotosPage.vue` |
| 说说管理 | `/admin/moments` | `MomentsPage.vue` |
| 友链管理 | `/admin/friend-links` | `FriendLinksPage.vue` |
| 访问记录 | `/admin/access-logs` | `AccessLogsPage.vue` |
| 赞助管理 | `/admin/sponsors` | `SponsorsPage.vue` |
| 内网穿透 | `/admin/nps` | `NpsPage.vue` |
| 系统设置 | `/admin/settings` | `SettingsPage.vue` |

其中 `NpsPage.vue` 下还拆分了几个子标签页：

- `nps/ClientsTab.vue`
- `nps/HostsTab.vue`
- `nps/ServerTab.vue`
- `nps/TunnelsTab.vue`

---

## 4. 权限与认证机制

## 4.1 登录态管理

前端登录态由 `client/src/stores/auth.ts` 管理，核心状态包括：

- `token`
- `user`
- `expiresAt`
- `menus`
- `menusCacheTime`

特性：

- 登录成功后保存 token 和用户信息
- 菜单数据有 **5 分钟缓存**
- 登录态做了持久化（`persist`）
- 退出登录时会清空 token 和菜单缓存

## 4.2 前端路由守卫

`client/src/router/index.ts` 中对后台访问做了以下控制：

- 访问后台受保护路由时，若未登录则跳转 `/admin/login`
- 进入 `/admin` 相关页面前会尝试拉取菜单
- 若访问的页面不在当前用户菜单权限内，则回退到后台首页 `/admin`

## 4.3 后端鉴权

`server/src/middleware/adminAuth.js` 对 `/api/admin` 路由做统一鉴权，特点包括：

- `/api/admin/login` 放行
- 其余 `/api/admin/*` 接口必须带 token
- token 支持从 `Authorization: Bearer xxx` 或 `x-admin-token` 中读取
- 若用户状态不是 `active`，会拒绝访问

## 4.4 登录安全控制

`server/src/controllers/adminAuthController.js` 中实现了登录安全逻辑：

- 账号密码必填校验
- 登录失败次数限制
- 同一 IP + 用户名连续失败过多会锁定
- 锁定时间为 **15 分钟**
- 登录成功后会记录 `lastLoginAt`

## 4.5 菜单与 RBAC 权限

后台菜单不是前端写死的，而是由后端根据用户角色动态返回：

- 用户拥有 `roleIds`
- 角色拥有 `menuIds`
- 后端根据角色聚合菜单
- 最终构造成树形结构返回给前端

对应接口：

- `GET /api/admin/menus`：获取当前用户菜单
- `GET /api/admin/roles`：角色列表
- `PUT /api/admin/roles/:id/menus`：分配角色菜单
- `GET /api/admin/menus/all`：菜单全集

说明：

- “仪表板”菜单会由后端固定插入为默认首页菜单
- 前端侧边栏菜单来自后端返回结果，而非本地静态配置

---

## 5. 登录方式说明

## 5.1 账号密码登录

前端调用：

- `POST /api/admin/login`

处理流程大致为：

1. 提交用户名和密码
2. 后端校验账号状态与密码
3. 签发后台 token
4. 返回用户信息与过期时间
5. 前端保存登录态并跳转后台首页

## 5.2 微信二维码登录

登录页 `AdminLogin.vue` 中集成了二维码登录逻辑，对应前端 API 在：

- `client/src/api/auth.ts`

主要流程：

1. `POST /api/auth/create-qr-session` 创建二维码会话
2. `GET /api/auth/generate-qr-code?qrToken=...` 获取二维码图片
3. 前端轮询 `GET /api/auth/check-qr-status/:qrToken`
4. 扫码后状态从 `pending` -> `scanned` -> `confirmed`
5. 确认成功后返回后台 token，前端完成登录

这部分说明后台系统已和微信侧身份体系做了打通。

---

## 6. 后端接口分层

## 6.1 管理后台主接口

主要前缀：`/api/admin`

典型路由文件：

- `server/src/routes/modules/adminAuth.js`
- `server/src/routes/modules/adminUsers.js`
- `server/src/routes/modules/adminMessages.js`
- `server/src/routes/modules/adminArticles.js`
- `server/src/routes/modules/adminPhotos.js`
- `server/src/routes/modules/adminMoments.js`
- `server/src/routes/modules/adminFriendLinks.js`
- `server/src/routes/modules/adminSponsors.js`
- `server/src/routes/modules/adminAccessLogs.js`
- `server/src/routes/modules/adminRbac.js`
- `server/src/routes/modules/adminSiteInfo.js`

这些接口主要负责后台 CRUD、权限管理与配置管理。

## 6.2 仪表板接口

前缀：`/api/admin/dashboard`

已确认接口：

- `GET /api/admin/dashboard/stats`

前端对应：

- `client/src/api/dashboard.ts`

返回结构包含：

- 留言统计
- 文章统计
- 相册统计
- 互动/反应统计
- 访问统计

说明仪表板主要承担后台总览作用。

## 6.3 NPS / 内网穿透管理接口

前缀：`/api/admin/nps`

路由文件：

- `server/src/routes/modules/nps.js`

能力包括：

- 服务端认证
- 仪表盘统计
- 隧道管理
- 域名解析管理
- 客户端管理

典型接口：

- `/auth/gettime`
- `/auth/getauthkey`
- `/dashboard/stats`
- `/tunnel/list`
- `/tunnel/add`
- `/tunnel/edit`
- `/tunnel/start`
- `/tunnel/stop`
- `/tunnel/delete`
- `/host/list`
- `/client/list`

说明这个后台不仅做内容管理，也承担了一部分运维管理能力。

## 6.4 文件管理接口

前缀：`/api/files`

路由文件：

- `server/src/fileManage/routes/index.js`

能力包括：

- 文件列表
- 存储占用统计
- 创建目录
- 重命名
- 删除/回收站/恢复/永久删除
- 搜索
- 收藏
- 树形目录
- 下载
- 上传
- 复制/移动
- 分片上传

典型接口：

- `GET /api/files/list`
- `GET /api/files/storage`
- `POST /api/files/folder`
- `PUT /api/files/:id/rename`
- `POST /api/files/delete`
- `GET /api/files/trash`
- `POST /api/files/restore`
- `POST /api/files/permanent-delete`
- `POST /api/files/upload/init`
- `POST /api/files/upload/chunk`
- `POST /api/files/upload/complete`

说明该模块虽然路由前缀不在 `/api/admin` 下，但本质仍依赖后台 token，是后台体系的重要组成部分。

---

## 7. 后台核心业务模块

从当前代码可归纳出后台主要覆盖以下业务：

### 7.1 内容管理

- 文章管理
- 相册管理
- 说说管理
- 留言管理
- 友情链接管理
- 赞助管理

### 7.2 系统管理

- 用户管理
- 角色管理
- 菜单管理
- 系统设置

### 7.3 数据与运营

- 仪表板统计
- 访问日志
- 互动数据统计

### 7.4 运维辅助

- NPS / 内网穿透管理
- 文件管理
- 上传任务管理

---

## 8. 关键文件位置

### 前端后台相关

```text
client/src/router/index.ts
client/src/stores/auth.ts
client/src/api/auth.ts
client/src/api/dashboard.ts
client/src/pages/admin/AdminLogin.vue
client/src/pages/admin/AdminLayout.vue
client/src/pages/admin/*.vue
client/src/components/admin/*
```

### 后端后台相关

```text
server/src/middleware/adminAuth.js
server/src/controllers/adminAuthController.js
server/src/controllers/dashboardController.js
server/src/controllers/*
server/src/routes/modules/admin*.js
server/src/routes/modules/dashboard.js
server/src/routes/modules/nps.js
server/src/routes/modules/wechatAuth.js
server/src/fileManage/routes/index.js
```

### 数据模型（重点）

```text
server/src/models/user.js
server/src/models/role.js
server/src/models/menu.js
server/src/models/loginAttempt.js
server/src/models/qrSession.js
server/src/models/accessLog.js
server/src/models/article.js
server/src/models/message.js
server/src/models/photo.js
server/src/models/moment.js
server/src/models/sponsor.js
```

---

## 9. 当前系统特征总结

基于现有代码，`homePage` 的后台管理系统有这些明显特征：

1. **不是简单的内容后台**，而是内容管理 + 权限系统 + 运维辅助的复合后台。
2. **具备 RBAC 能力**，用户、角色、菜单是成体系的。
3. **支持二维码登录**，说明后台登录体验做得比较完整。
4. **支持动态菜单**，前端页面访问受后端权限结果控制。
5. **包含 NPS 管理与文件管理**，说明这个后台已经部分承担私有运维面板的职责。
6. **接口和页面拆分较清晰**，后续适合继续扩展模块。

---

## 10. 如果后续要继续补文档，建议再补的几份

如果你后面要把这套后台文档写完整，建议继续拆成：

1. `后台权限设计说明.md`
   - 用户/角色/菜单关系
   - 菜单树构建逻辑
   - 权限校验策略

2. `后台接口清单.md`
   - 各模块 API 明细
   - 请求参数 / 返回结构 / 鉴权要求

3. `后台登录与扫码流程.md`
   - 账号密码登录
   - 微信二维码登录
   - Token 刷新与失效处理

4. `后台文件管理模块说明.md`
   - 文件上传
   - 回收站
   - 分片上传
   - 权限依赖

5. `后台 NPS 模块说明.md`
   - 隧道、客户端、域名管理
   - 和现网环境的关系

---

## 11. 本文依据的主要代码位置

- `client/src/router/index.ts`
- `client/src/stores/auth.ts`
- `client/src/pages/admin/AdminLayout.vue`
- `client/src/pages/admin/AdminLogin.vue`
- `client/src/api/auth.ts`
- `client/src/api/dashboard.ts`
- `server/src/routes/index.js`
- `server/src/routes/modules/adminAuth.js`
- `server/src/routes/modules/adminRbac.js`
- `server/src/routes/modules/dashboard.js`
- `server/src/routes/modules/nps.js`
- `server/src/routes/modules/wechatAuth.js`
- `server/src/fileManage/routes/index.js`
- `server/src/middleware/adminAuth.js`
- `server/src/controllers/adminAuthController.js`

---

如果你要，我下一步可以继续直接给你补：

- **更偏产品视角的版本**（给你自己看）
- **更偏技术设计视角的版本**（给开发用）
- **接口清单版**（适合联调和后续接手）
