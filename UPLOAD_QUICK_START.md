# 快速启动指南

## 配置上传目录到外接硬盘 ✅

您已经成功配置了上传目录到外接硬盘 `/Volumes/大金刚/uploads`

### 当前配置

```env
UPLOAD_DIR="/Volumes/大金刚/uploads"
UPLOAD_BASE_URL="/uploads"
```

### 启动服务

```bash
cd /Users/giovan/Desktop/homePage/server

# 开发模式
pnpm dev

# 或生产模式
pnpm start
```

### 验证配置

上传成功后，您会看到：
- ✅ 文件保存在 `/Volumes/大金刚/uploads/` 目录中
- ✅ 访问 URL 格式：`http://localhost:8998/uploads/[filename]`
- ✅ 外接硬盘会显示上传的文件

## 工作流程

1. **上传文件**
   - 通过管理后台上传头像或其他文件
   - 文件自动保存到外接硬盘

2. **访问文件**
   - 浏览器通过 `/uploads/[filename]` 访问文件
   - 后端从 `/Volumes/大金刚/uploads/` 读取文件

3. **删除文件**
   - 通过管理后台删除文件
   - 文件从外接硬盘中移除

## 相关文件

- 📄 详细配置文档: [UPLOAD_CONFIG.md](UPLOAD_CONFIG.md)
- 📝 环境变量: [.env](.env)
- 🔧 后端配置: [server/src/app.js](server/src/app.js)
- 📤 上传控制器: [server/src/controllers/uploadController.js](server/src/controllers/uploadController.js)

## 常见操作

### 改变上传目录

编辑 `.env` 文件：
```env
# 改为其他路径
UPLOAD_DIR="/your/new/path/uploads"
```

然后重启后端服务。

### 查看已上传的文件

```bash
# 使用终端查看
ls -lah "/Volumes/大金刚/uploads/"

# 或在 Finder 中打开
open "/Volumes/大金刚/uploads/"
```

### 清空上传文件

```bash
# 谨慎操作！删除所有上传的文件
rm -rf "/Volumes/大金刚/uploads"/* 

# 重新创建目录
mkdir -p "/Volumes/大金刚/uploads"
```

## 故障排查

| 问题 | 解决方案 |
|------|--------|
| 外接硬盘不显示 | 检查硬盘是否连接，使用 `diskutil list` 查看 |
| 上传失败 | 检查目录权限：`chmod 755 "/Volumes/大金刚/uploads"` |
| 文件访问 404 | 确保后端服务正在运行，检查 `UPLOAD_DIR` 路径 |

---

📚 更多信息请查看 [UPLOAD_CONFIG.md](UPLOAD_CONFIG.md)
